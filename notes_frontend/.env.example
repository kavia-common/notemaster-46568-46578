# Frontend environment variables
# Set the base origin (protocol+host+port) for the backend API.
# The client will automatically prefix /api to match FastAPI routes.
REACT_APP_API_BASE=http://localhost:3001
